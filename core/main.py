[
    {"path": "chorus-trainer/requirements.txt", "content": "numpy>=1.21.0\nscipy>=1.7.0\nsounddevice>=0.4.4\nmatplotlib>=3.4.0\npydub>=0.25.1\npytest>=7.0.0\n"},
    {"path": "chorus-trainer/README.md", "content": "# Chorus Trainer\n\nA Python tool to help singers practice and improve chorus parts. Analyzes audio input, provides pitch correction feedback, and tracks progress over time.\n\n## Features\n- Real-time pitch detection from microphone or audio file\n- Compare sung notes to reference chorus sheet music\n- Visual feedback on pitch accuracy\n- Practice session logging and progress tracking\n\n## Installation\n```\npip install -r requirements.txt\n```\n\n## Usage\n```python\nfrom chorus_trainer import ChorusTrainer\n\n# Load a reference chorus part (MIDI or sheet music)\ntrainer = ChorusTrainer(reference=\"twinkle_twinkle.mid\")\n\n# Start a practice session\ntrainer.start_session()\n```\n"},
    {"path": "chorus-trainer/chorus_trainer/__init__.py", "content": "from .core import ChorusTrainer\nfrom .pitch_detector import PitchDetector\nfrom .analyzer import PitchAnalyzer\n\n__all__ = [\"ChorusTrainer\", \"PitchDetector\", \"PitchAnalyzer\"]\n"},
    {"path": "chorus-trainer/chorus_trainer/pitch_detector.py", "content": "\"\"\"Pitch detection module using autocorrelation.\"\"\"\n\nimport numpy as np\nfrom scipy.signal import correlate\n\n\nclass PitchDetector:\n    \"\"\"Detect fundamental frequency from audio samples.\"\"\"\n\n    def __init__(self, sample_rate: int = 44100, min_freq: float = 65.0, max_freq: float = 1047.0):\n        \"\"\"\n        Args:\n            sample_rate: Audio sample rate in Hz.\n            min_freq: Minimum detectable frequency (Hz).\n            max_freq: Maximum detectable frequency (Hz).\n        \"\"\"\n        self.sample_rate = sample_rate\n        self.min_freq = min_freq\n        self.max_freq = max_freq\n\n    def detect(self, audio: np.ndarray) -> float:\n        \"\"\"\n        Detect the fundamental frequency of an audio signal.\n\n        Uses normalized autocorrelation to find pitch period.\n\n        Args:\n            audio: 1D numpy array of audio samples.\n\n        Returns:\n            Detected frequency in Hz, or 0.0 if no pitch found.\n        \"\"\"\n        if len(audio) < 2:\n            return 0.0\n\n        # Apply Hanning window to reduce spectral leakage\n        windowed = audio * np.hanning(len(audio))\n\n        # Compute autocorrelation\n        correlation = correlate(windowed, windowed, mode='full')\n        correlation = correlation[len(correlation) // 2:]  # Keep only positive lags\n\n        # Find the first peak after the zero-lag point\n        min_lag = int(self.sample_rate / self.max_freq)\n        max_lag = int(self.sample_rate / self.min_freq)\n\n        if max_lag >= len(correlation):\n            max_lag = len(correlation) - 1\n        if min_lag >= max_lag:\n            return 0.0\n\n        # Normalize by dividing by correlation at zero lag\n        zero_lag = correlation[0]\n        if zero_lag == 0:\n            return 0.0\n        normalized = correlation[min_lag:max_lag] / zero_lag\n\n        # Find the highest peak in the normalized correlation\n        peak_index = np.argmax(normalized)\n        peak_value = normalized[peak_index]\n\n        if peak_value < 0.1:  # Threshold to avoid noise peaks\n            return 0.0\n\n        # Convert lag to frequency\n        lag = min_lag + peak_index\n        if lag == 0:\n            return 0.0\n        frequency = self.sample_rate / lag\n\n        return frequency\n\n    def freq_to_note(self, freq: float) -> str:\n        \"\"\"\n        Convert frequency to nearest note name (e.g., 'A4', 'C#5').\n\n        Args:\n            freq: Frequency in Hz.\n\n        Returns:\n            Note name string.\n        \"\"\"\n        if freq <= 0:\n            return \"--\"\n\n        # A4 = 440 Hz, MIDI note 69\n        midi_note = 69 + 12 * np.log2(freq / 440.0)\n        midi_rounded = int(round(midi_note))\n\n        note_names = ['C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B']\n        octave = midi_rounded // 12 - 1\n        note_idx = midi_rounded % 12\n\n        return f\"{note_names[note_idx]}{octave}\"\n"},
    {"path": "chorus-trainer/chorus_trainer/analyzer.py", "content": "\"\"\"Pitch accuracy analyzer comparing sung notes to reference.\"\"\"\n\nimport numpy as np\n\n\nclass PitchAnalyzer:\n    \"\"\"Compare detected pitch against reference notes and compute accuracy.\"\"\"\n\n    def __init__(self, reference_notes: list[float], tolerance_cents: float = 50.0):\n        \"\"\"\n        Args:\n            reference_notes: List of reference frequencies in Hz.\n            tolerance_cents: Acceptable deviation in cents (100 cents = 1 semitone).\n        \"\"\"\n        self.reference_notes = np.array(reference_notes)\n        self.tolerance_cents = tolerance_cents\n\n    def cents_off(self, sung_freq: float, ref_freq: float) -> float:\n        \"\"\"\n        Calculate how many cents the sung frequency is from reference.\n\n        Args:\n            sung_freq: Detected frequency in Hz.\n            ref_freq: Reference frequency in Hz.\n\n        Returns:\n            Deviation in cents (positive = sharp, negative = flat).\n        \"\"\"\n        if sung_freq <= 0 or ref_freq <= 0:\n            return float('inf')\n        return 1200 * np.log2(sung_freq / ref_freq)\n\n    def analyze_note(self, sung_freq: float, ref_freq: float) -> dict:\n        \"\"\"\n        Analyze a single sung note against its reference.\n\n        Args:\n            sung_freq: Detected frequency.\n            ref_freq: Expected reference frequency.\n\n        Returns:\n            Dictionary with 'cents_deviation', 'is_accurate', 'sung_freq', 'ref_freq'.\n        \"\"\"\n        deviation = self.cents_off(sung_freq, ref_freq)\n        is_accurate = abs(deviation) <= self.tolerance_cents\n        return {\n            'cents_deviation': deviation,\n            'is_accurate': is_accurate,\n            'sung_freq': sung_freq,\n            'ref_freq': ref_freq\n        }\n\n    def analyze_sequence(self, sung_notes: list[float]) -> list[dict]:\n        \"\"\"\n        Analyze a sequence of sung notes against reference notes.\n\n        Args:\n            sung_notes: List of detected frequencies (same length as reference).\n\n        Returns:\n            List of analysis dictionaries.\n        \"\"\"\n        results = []\n        for sung, ref in zip(sung_notes, self.reference_notes):\n            results.append(self.analyze_note(sung, ref))\n        return results\n\n    def accuracy_score(self, sung_notes: list[float]) -> float:\n        \"\"\"\n        Compute overall accuracy percentage based on tolerance.\n\n        Args:\n            sung_notes: List of detected frequencies.\n\n        Returns:\n            Percentage of notes within tolerance (0.0 to 100.0).\n        \"\"\"\n        if not sung_notes or len(self.reference_notes) == 0:\n            return 0.0\n        results = self.analyze_sequence(sung_notes)\n        accurate = sum(1 for r in results if r['is_accurate'])\n        return (accurate / len(results)) * 100.0\n"},
    {"path": "chorus-trainer/chorus_trainer/core.py", "content": "\"\"\"Main ChorusTrainer class for practice sessions.\"\"\"\n\nimport time\nimport numpy as np\nfrom .pitch_detector import PitchDetector\nfrom .analyzer import PitchAnalyzer\n\n\nclass ChorusTrainer:\n    \"\"\"\n    Main class to manage chorus practice sessions.\n\n    Handles audio input, pitch detection, and progress tracking.\n    \"\"\"\n\n    def __init__(self, reference_notes: list[float], sample_rate: int = 44100):\n        \"\"\"\n        Args:\n            reference_notes: List of reference frequencies (Hz) for the chorus part.\n            sample_rate: Audio sample rate for detection.\n        \"\"\"\n        self.reference_notes = reference_notes\n        self.sample_rate = sample_rate\n        self.pitch_detector = PitchDetector(sample_rate=sample_rate)\n        self.analyzer = PitchAnalyzer(reference_notes)\n        self.session_history: list[dict] = []\n\n    def start_session(self) -> None:\n        \"\"\"Start a new practice session (placeholder for real-time audio capture).\"\"\"\n        self.session_history = []\n        print(\"Session started. Ready for audio input.\")\n\n    def process_audio(self, audio_chunk: np.ndarray) -> dict:\n        \"\"\"\n        Process a chunk of audio and return pitch analysis.\n\n        Args:\n            audio_chunk: 1D numpy array of audio samples.\n\n        Returns:\n            Dictionary with detected frequency, note name, and accuracy info.\n        \"\"\"\n        freq = self.pitch_detector.detect(audio_chunk)\n        note = self.pitch_detector.freq_to_note(freq)\n\n        # Compare to first unprocessed reference note or nearest\n        if self.reference_notes:\n            # Use the next reference note based on session progress\n            ref_idx = len(self.session_history)\n            if ref_idx >= len(self.reference_notes):\n                ref_idx = len(self.reference_notes) - 1\n            ref_freq = self.reference_notes[ref_idx]\n            analysis = self.analyzer.analyze_note(freq, ref_freq)\n        else:\n            analysis = {'cents_deviation': 0.0, 'is_accurate': False, 'sung_freq': freq, 'ref_freq': 0.0}\n\n        result = {\n            'timestamp': time.time(),\n            'frequency': freq,\n            'note': note,\n            'analysis': analysis\n        }\n        self.session_history.append(result)\n        return result\n\n    def get_progress_summary(self) -> dict:\n        \"\"\"\n        Get a summary of the current practice session.\n\n        Returns:\n            Dictionary with total notes, accuracy, and recent history.\n        \"\"\"\n        if not self.session_history:\n            return {'total_notes': 0, 'accuracy': 0.0, 'history': []}\n\n        sung_freqs = [h['frequency'] for h in self.session_history]\n        accuracy = self.analyzer.accuracy_score(sung_freqs)\n        return {\n            'total_notes': len(self.session_history),\n            'accuracy': accuracy,\n            'history': self.session_history[-10:]  # Last 10 entries\n        }\n\n    def end_session(self) -> dict:\n        \"\"\"\n        End the practice session and return final statistics.\n\n        Returns:\n            Dictionary with session stats.\n        \"\"\"\n        summary = self.get_progress_summary()\n        print(f\"Session ended. Accuracy: {summary['accuracy']:.1f}%\")\n        return summary\n"},
    {"path": "chorus-trainer/tests/test_pitch_detector.py", "content": "\"\"\"Tests for PitchDetector class.\"\"\"\n\nimport numpy as np\nimport pytest\nfrom chorus_trainer.pitch_detector import PitchDetector\n\n\ndef test_detect_sine_wave():\n    \"\"\"Test pitch detection on a pure sine wave.\"\"\"\n    sample_rate = 44100\n    duration = 0.1  # seconds\n    freq = 440.0  # A4\n    t = np.linspace(0, duration, int(sample_rate * duration), endpoint=False)\n    audio = np.sin(2 * np.pi * freq * t)\n\n    detector = PitchDetector(sample_rate=sample_rate)\n    detected = detector.detect(audio)\n\n    # Allow 1% error due to windowing and discrete sampling\n    assert abs(detected - freq) / freq < 0.01, f\"Detected {detected} Hz, expected {freq} Hz\"\n\n\ndef test_detect_silence():\n    \"\"\"Test that silence returns 0.0 Hz.\"\"\"\n    audio = np.zeros(4410)  # 0.1 seconds of silence\n    detector = PitchDetector()\n    detected = detector.detect(audio)\n    assert detected == 0.0, f\"Expected 0.0 for silence, got {detected}\"\n\n\ndef test_freq_to_note_a4():\n    \"\"\"Test frequency to note conversion for A4 (440 Hz).\"\"\"\n    detector = PitchDetector()\n    note = detector.freq_to_note(440.0)\n    assert note == \"A4\", f\"Expected A4, got {note}\"\n\n\ndef test_freq_to_note_c4():\n    \"\"\"Test frequency to note conversion for middle C (261.63 Hz).\"\"\"\n    detector = PitchDetector()\n    note = detector.freq_to_note(261.63)\n    assert note == \"C4\", f\"Expected C4, got {note}\"\n\n\ndef test_freq_to_note_negative():\n    \"\"\"Test that negative frequency returns '--'.\"\"\"\n    detector = PitchDetector()\n    note = detector.freq_to_note(-100)\n    assert note == \"--\", f\"Expected '--', got {note}\"\n\n\ndef test_detect_different_frequencies():\n    \"\"\"Test detection across multiple frequencies.\"\"\"\n    sample_rate = 44100\n    detector = PitchDetector(sample_rate=sample_rate)\n    test_freqs = [130.81, 261.63, 523.25, 1046.50]  # C3, C4, C5, C6\n\n    for freq in test_freqs:\n        duration = 0.1\n        t = np.linspace(0, duration, int(sample_rate * duration), endpoint=False)\n        audio = np.sin(2 * np.pi * freq * t)\n        detected = detector.detect(audio)\n        assert abs(detected - freq) / freq < 0.02, f\"Failed for {freq} Hz, got {detected} Hz\"\n"},
    {"path": "chorus-trainer/tests/test_analyzer.py", "content": "\"\"\"Tests for PitchAnalyzer class.\"\"\"\n\nimport pytest\nfrom chorus_trainer.analyzer import PitchAnalyzer\n\n\ndef test_cents_off_exact():\n    \"\"\"Test cents_off with exact match.\"\"\"\n    analyzer = PitchAnalyzer([440.0])\n    deviation = analyzer.cents_off(440.0, 440.0)\n    assert deviation == 0.0, f\"Expected 0.0, got {deviation}\"\n\n\ndef test_cents_off_sharp():\n    \"\"\"Test cents_off when sung is sharp.\"\"\"\n    analyzer = PitchAnalyzer([440.0])\n    deviation = analyzer.cents_off(466.16, 440.0)  # ~100 cents sharp (A4 to A#4)\n    assert deviation > 90 and deviation < 110, f\"Expected ~100, got {deviation}\"\n\n\ndef test_cents_off_flat():\n    \"\"\"Test cents_off when sung is flat.\"\"\"\n    analyzer = PitchAnalyzer([440.0])\n    deviation = analyzer.cents_off(415.30, 440.0)  # ~-100 cents flat (A4 to G#4)\n    assert deviation < -90 and deviation > -110, f\"Expected ~-100, got {deviation}\"\n\n\ndef test_analyze_note_accurate():\n    \"\"\"Test analyze_note returns is_accurate=True for in-tune note.\"\"\"\n    analyzer = PitchAnalyzer([440.0], tolerance_cents=50.0)\n    result = analyzer.analyze_note(442.0, 440.0)  # ~7.8 cents sharp\n    assert result['is_accurate'] is True\n
